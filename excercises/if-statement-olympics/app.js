if (5 > 3){ 
    console.log("is greater than")
    }

    var cat={
    length: 3,
    }

    if (cat.length === 3){
    console.log ("is the length")
}

if ("cat" === "dog"){
    console.log ("the same")
} else {
    console.log("they are not equal")
 }

var person={
    firstName: "bobby",
    age: 12, 
    charAt: 0,
}

if (person.name === "bobby"){
    console.log(person.firstName + " is allowed to go to the movie")
}

if (person.charAt === "b" && person.age > 18){
    console.log(person.firstName + "is allowed to go to the movie")
}
if ("1" === "1"){
    console.log("loose" || "abstract")}
    else {
    console.log ("not equal at all")
}

// if ("1" !== 2 * 2 = 4 ){
//     console.log ("yes")
// }